name: CICD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # - uses: actions/setup-node@v1

      - name: Install
        run: npm install

      # - name: Lint
      #   run: npm run lint

      - name: Build
        run: npm run dev-build
